/*
import { defineConfig } from 'vite';

export default defineConfig({
  base: '/', 
  build: {
    outDir: 'dist',
    assetsDir: 'assets',
  }
});
*/
// Ce fichier est désactivé car le projet fonctionne désormais en mode statique (HTML/JS) sans étape de build Vite.
// Cela permet d'éviter les erreurs de dépendances manquantes sur GitHub/CI.
